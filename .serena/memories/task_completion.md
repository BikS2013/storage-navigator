# Task completion
- Prefer `npm run build` as the available compile-time validation.
- No working automated test suite is configured in `package.json`; `npm test` intentionally exits with an error.
- For reviews, verify branch state with `git status` and inspect diffs against `main` before drawing conclusions.
