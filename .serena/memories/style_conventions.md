# Style and conventions
- Language: TypeScript with `strict` enabled.
- Module system: ESM (`"type": "module"`, `module`/`moduleResolution` Node16).
- Naming/layout: command implementations live under `src/cli/commands`, shared logic under `src/core`, Electron code under `src/electron`.
- No dedicated formatter/linter config found at repo root during onboarding.
